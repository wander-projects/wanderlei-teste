{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="col-md-10 col-md-offset-1">
            <h3>Editar atividade</h3>
            <div class="panel panel-default">
                <div class="panel-heading">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item"><a href="#">Atividades</a></li>
                        <li class="breadcrumb-item"><a href="{{path('activity_index')}}">Listagem</a></li>
                        <li class="breadcrumb-item active">Editar</li>
                    </ol>
                </div>
                <div class="panel-body">
                    {{ form_start(edit_form, {'attr': {'novalidate': 'novalidate'}}) }}
                    <div class="form-group">
                    {{ form_widget(edit_form.name, {'attr': {'class': 'form-control', 'placeholder':'Nome'}}) }}
                    </div>
                    <div class="form-group">
                    {{ form_widget(edit_form.status, {'attr': {'class': 'form-control'}}) }}
                    </div>
                    <div class="form-group">
                    {{ form_widget(edit_form.situation, {'attr': {'class': 'form-control'}}) }}
                    </div>
                    <div class="form-group">
                    {{ form_widget(edit_form.startDate, {'attr': {'class': 'datepickerStart form-control', 'placeholder':'Data de inicio'}}) }}
                    </div>
                    <div class="form-group" id="endDate" style="display: none">
                    {{ form_widget(edit_form.endDate, {'attr': {'class': 'datepickerEnd form-control', 'placeholder':'Data de fim'}}) }}
                    </div>
                    <div class="form-group">
                    {{ form_widget(edit_form.description, {'attr': {'class': 'form-control', 'placeholder':'Descrição'}}) }}
                    </div>
                </div>
                <div class="panel-footer">
                    <input type="submit" value="Salvar" class="btn btn-primary" />
                {{ form_end(edit_form) }}
                <div class="pull-right">
                    <a href="{{ path('activity_index') }}">voltar a lista de atividades</a>
                </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
